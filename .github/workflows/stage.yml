name: 23people-company-handbook

on:
  push:
    branches: 
      - stage
   
jobs:
  build:
    runs-on: ubuntu-latest
    environment: stage
    steps:
      - name: Checkout repository into Github Runner
        uses: actions/checkout@v4
      
      - name: Set up SSH in the Github Runner
        run: |
          # Creates a directory for the SSH key
          mkdir -p ~/.ssh

          # Adds the SSH key to the Github Runner
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key

          # Changes the permissions of the SSH key
          chmod 600 ~/.ssh/deploy_key

          echo "Deploy Key created."

      - name: Add Deployment Host to known_hosts file in the Github Runner
        run: |
          # Adds the Deployment Host to the known_hosts file
          ssh-keyscan -H ${{ secrets.DEPLOYMENT_HOST }} >> ~/.ssh/known_hosts

          echo "Added ${{ secrets.DEPLOYMENT_HOST }} to known hosts."
          cat ~/.ssh/known_hosts

      - name: Deploy code into Deployment Host (via SSH from the Github Runner)
        run: |
          # Deploys the code into the Deployment Host
          ssh -i ~/.ssh/deploy_key ${{ secrets.SSH_USERNAME }}@${{ secrets.DEPLOYMENT_HOST }} '
            mkdir -p ${{ vars.WORKDIR }} &&
            cd ${{ vars.WORKDIR }} &&
            echo "Hola!"
          '
      - name: Install dependencies into Deployment Host (via SSH from the Github Runner)
        run: |
          # Installs the dependencies into the Deployment Host
          ssh -i ~/.ssh/deploy_key ${{ secrets.SSH_USERNAME }}@${{ secrets.DEPLOYMENT_HOST }} '
            cd ${{ vars.WORKDIR }} &&
            /home/${{ secrets.SSH_USERNAME }}/.local/bin/poetry install --no-root &&
            export PYTHONPATH=./src
          '
      - name: Restart pm2 in the Deployment Host (via SSH from the Github Runner)
        run: |
          # Restarts the pm2 in the Deployment Host
          ssh -i ~/.ssh/deploy_key ${{ secrets.SSH_USERNAME }}@${{ secrets.DEPLOYMENT_HOST }} '
            cd ${{ vars.WORKDIR }} &&
            source ~/.nvm/nvm.sh &&
            pm2 restart run-pm2-${{ github.ref_name }}.json
          '
        
