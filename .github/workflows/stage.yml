name: 23people-company-handbook

on:
  push:
    branches: 
      - stage


   
jobs:
  build:
    runs-on: ubuntu-latest
    environment: stage
    env:
      SSH_PATH: '~/.ssh'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up SSH
        run: |
          mkdir -p $SSH_PATH
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > $SSH_PATH/deploy_key
          chmod 600 $SSH_PATH/deploy_key

      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.IP_SERVER }} >> $SSH_PATH/known_hosts
          echo "Done"

      - name: Deploy code via SSH
        run: |
          ssh -i $DEPLOY_KEY_PATH ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} '
            mkdir -p ${{ vars.WORKDIR }} &&
            cd ${{ vars.WORKDIR }} &&
            echo "Hola!"
          '
      - name: Install dependencies
        run: |
          ssh -i $SSH_PATH/deploy_key ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} '
            cd ${{ vars.WORKDIR }} &&
            /home/${{ secrets.SSH_USERNAME }}/.local/bin/poetry install --no-root &&
            export PYTHONPATH=./src
          '
      - name: Restart pm2
        run: |
          ssh -i $SSH_PATH/deploy_key ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} '
            cd ${{ vars.WORKDIR }} &&
            source ~/.nvm/nvm.sh &&
            pm2 restart run-pm2-${{ github.ref_name }}.json
          '
        
